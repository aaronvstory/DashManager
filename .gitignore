# ── Data & secrets (NEVER commit — real customer data) ──────────────
data/
*.db
*.db-wal
*.db-shm
*cookies*.json
*storage*.json
session_*.json
profiles/
ss/
screenshots/
.env
.env.*
!.env.example

# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# ── Node / frontend ──────────────────────────────────────────────────
node_modules/
frontend/dist/
frontend/.vite/
*.local

# ── OS / editor junk ─────────────────────────────────────────────────
nul
Thumbs.db
Desktop.ini
.DS_Store
*.log
logs/

# ── Session state ────────────────────────────────────────────────────
.claude/

# Throwaway live-test / scratch scripts + artifacts (anything _-prefixed in
# repo root: _cdp_*.py, _live_*.py, _sig*.txt, _*.png, ...). Real modules live
# under backend/ and tests/, never as a leading-underscore root file.
_*.py
_*.txt
_*.png
_*.json
_live_test_*.py
_dump_*.py
_*_out.txt
_codex_*.txt

# SeleniumBase downloads/artifacts dir
downloaded_files/
latest_logs/
assets/
.unittest-marker

# Serena project cache
.serena/

# Codex review logs
codex_*.log
.codex_*

# Proxy credentials — NEVER commit (residential proxy creds)
working-proxies.txt
*proxies*.txt
proxies.json
